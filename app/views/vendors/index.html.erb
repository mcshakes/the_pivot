<h1>All Our Photographers</h1>

<div class="vendor-container">
  <ul id="list">
    <% @vendors.each do |vendor| %>
    <div class="category col-md-5 col-md-offset-1">
      <li> <%= link_to "#{vendor.name}", vendor %> </li>
    </div>
    <% end %>
  </ul>
  <div style="clear:both"> </div>
</div>
